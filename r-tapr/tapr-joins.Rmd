---
title: "TAPR data joins"
output: html_notebook
---
```{r}
library(tidyverse)
library(janitor)
library(lubridate)
library(readxl)
library("rio")

```

```{r}
read_excel(
  "data-raw/DSTUD_2013_2014.xlsx"
  )
```

# converting xlsx to csv using rio  
```{r} 
xls <- dir(pattern = "xlsx")
created <- mapply(convert, xls, gsub("xlsx", "csv", xls))
```
